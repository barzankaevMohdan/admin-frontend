# 🚀 Admin Frontend - Установка и запуск на Windows

Административная панель для управления системой учета посещаемости.

---

## 📋 Требования

### Node.js и npm
**Версия:** Node.js 18.x или выше

**Скачать:**
- [Node.js 18 LTS](https://nodejs.org/en/download/) (рекомендуется)
- [Node.js 20 LTS](https://nodejs.org/en/download/) (актуальная версия)

**Проверка установки:**
```powershell
node --version
npm --version
```

---

## 🔧 Установка и настройка

### Шаг 1: Установка зависимостей

```powershell
cd admin-frontend
npm install
```

---

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` на основе `env.example`:
```powershell
copy env.example .env
```

Откройте `.env` и укажите:
```env
VITE_API_BASE_URL=http://localhost:3000
```

> 💡 Убедитесь, что Backend запущен на порту 3000

---

## ▶️ Запуск

### Режим разработки
```powershell
npm run dev
```

Или укажите конкретный хост и порт:
```powershell
npm run dev -- --host 127.0.0.1 --port 8080
```

Интерфейс доступен на: `http://localhost:5173` (или `http://localhost:8080`)

### Продакшен

```powershell
# Сборка
npm run build

# Предпросмотр сборки
npm run preview
```

---

## 🔐 Вход в систему

### Суперадмин
- Email: `superadmin@system.com`
- Пароль: `SuperAdmin123!`
- Доступ ко всем компаниям

### Админ компании
- Email: `admin@demo.com`
- Пароль: `Admin123!`
- Доступ только к своей компании

---

## 🎯 Основные функции

### Для Суперадмина:
- ✅ Управление компаниями
- ✅ Управление пользователями всех компаний
- ✅ Просмотр статистики по всем компаниям
- ✅ Системные настройки

### Для Админа компании:
- ✅ Управление камерами компании
- ✅ Управление сотрудниками
- ✅ Просмотр событий распознавания
- ✅ Статистика посещаемости
- ✅ Управление пользователями компании
- ✅ Live просмотр камер

---

## 🛠️ Полезные команды

```powershell
# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview

# Проверка типов
npm run type-check

# Линтинг
npm run lint
```

---

## 🐛 Решение проблем

### Ошибка: "Failed to fetch"
- Убедитесь, что Backend запущен на `http://localhost:3000`
- Проверьте `VITE_API_BASE_URL` в `.env` файле
- Откройте DevTools (F12) и посмотрите Network вкладку

### Порт 5173 уже занят
```powershell
# Найти процесс на порту 5173
netstat -ano | findstr :5173

# Завершить процесс по PID
taskkill /PID <PID> /F

# Или используйте другой порт
npm run dev -- --port 8080
```

### Ошибка: "Cannot find module"
```powershell
# Очистка и переустановка зависимостей
rmdir /s /q node_modules
del package-lock.json
npm install
```

### Проблемы с CORS
- Убедитесь, что в Backend `.env` установлено:
  ```env
  CORS_ORIGIN=*
  # или конкретный origin
  CORS_ORIGIN=http://localhost:5173
  ```

---

## 🏗️ Структура проекта

```
admin-frontend/
├── src/
│   ├── api/                   # API клиент
│   │   └── client.ts
│   ├── components/            # Vue компоненты
│   ├── layouts/               # Макеты страниц
│   │   └── MainLayout.vue
│   ├── router/                # Vue Router
│   │   └── index.ts
│   ├── stores/                # Pinia stores (state)
│   │   └── auth.ts
│   ├── views/                 # Страницы
│   │   ├── Login.vue
│   │   ├── Dashboard.vue
│   │   ├── Cameras.vue
│   │   ├── Companies.vue
│   │   ├── Employees.vue
│   │   ├── Events.vue
│   │   ├── Live.vue
│   │   ├── Presence.vue
│   │   └── Statistics.vue
│   ├── App.vue                # Корневой компонент
│   ├── main.ts                # Точка входа
│   └── style.css              # Глобальные стили
├── .env                       # Переменные окружения
├── index.html
├── package.json
├── vite.config.ts             # Vite конфигурация
└── tsconfig.json
```

---

## 🎨 Технологический стек

- **Vue.js 3** - UI фреймворк
- **TypeScript** - Типизация
- **Vite** - Build tool и dev server
- **Vue Router** - Роутинг
- **Pinia** - State management
- **Axios** - HTTP клиент
- **TailwindCSS** - CSS фреймворк (если используется)

---

## 📚 Полезные ссылки

- [Vue.js 3 документация](https://vuejs.org/)
- [Vite документация](https://vitejs.dev/)
- [Pinia документация](https://pinia.vuejs.org/)
- [Vue Router документация](https://router.vuejs.org/)

---

## 💡 Советы для разработки

1. **Используйте PowerShell 7** для лучшей совместимости

2. **Vue DevTools** для отладки:
   - [Chrome Extension](https://chrome.google.com/webstore/detail/vuejs-devtools/nhdogjmejiglipccpnnnanhbledajbpd)
   - [Firefox Extension](https://addons.mozilla.org/en-US/firefox/addon/vue-js-devtools/)

3. **Hot Module Replacement (HMR)** работает автоматически в dev режиме

4. **TypeScript проверки** можно запустить отдельно:
   ```powershell
   npm run type-check
   ```

5. **Добавьте `node_modules` в исключения антивируса** для ускорения работы

---

Если возникли проблемы, откройте Issue в репозитории! 🚀
